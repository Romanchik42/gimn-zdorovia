═══════════════════════════════════════════════════════════════════
ТИКЕТ GIMN-013 — Визуал на КАЖДОЕ упражнение (Pixabay + рисованные SVG)
═══════════════════════════════════════════════════════════════════

ШАПКА (источники истины, полными путями):
- SPEC: F:\Project gimn zdorovia\SPEC_gimn_zdorovia_v2.md (читай перед работой; SPEC.md нет — файл называется так)
- Проект: F:\Project gimn zdorovia
- Секреты: F:\Project gimn zdorovia\secrets\.env  ← там теперь есть PIXABAY_API_KEY
- Прод: https://gimn-zdorovia.vercel.app
- База: 22 таблицы, RLS (после GIMN-012, миграции до 0015), 2 реальных пользователя — НЕ трогать
- Автодеплой: GitHub Actions (не трогать логику)

ЦЕЛЬ: сейчас визуал есть только у 8 упражнений из 50 (живые GIF), остальные
42 показывают знак типа. Нужно, чтобы на КАЖДОМ упражнении был визуал.
Путь 3 (выбор Романа): где есть точное совпадение — живая гифка/картинка с
Pixabay; где нет — аккуратная рисованная SVG-схема движения со стрелками.

КОНТЕКСТ (что уже есть, НЕ переделывать):
- 8 GIF лежат в public/exercises/ (7 из серии CDC — public domain, 1 дыхание CC0),
  привязаны к exercises.gif_url / image_url.
- Есть компонент-заглушка src/components/exercise/exercise-placeholder.tsx
  (знак по типу: дыхание/разминка/основное/растяжка).
- Источники и лицензии ведутся в docs/IMAGE_SOURCES.md.
- Карточка упражнения exercise-card.tsx показывает gif/картинку сверху,
  lazy-load, Skeleton (SPEC 4.3).
- Роман просил: гифки понравились, но их мало — хочет на каждое занятие.
  Отметил, что тренировка ~2 минуты на упражнение (визуал должен помогать
  делать движение, а не грузить).

ШАГ 0 — РАЗВЕДКА ФАКТОМ (покажи в чат):
- Список всех 50 упражнений из справочника: id, name, type, target_joint,
  есть ли сейчас gif_url/image_url (SQL SELECT).
- Как exercise-card.tsx выбирает что показать (gif → image → placeholder).
- Содержимое docs/IMAGE_SOURCES.md (что уже взято и откуда).
- Проверь, что PIXABAY_API_KEY читается из .env (не печатай сам ключ в чат —
  только подтверди, что переменная присутствует и не пустая).
Не выдумывай.

═══════════════════════════════════════════════════════════════════
БЛОК A — ЖИВЫЕ КАРТИНКИ/ГИФКИ С PIXABAY
═══════════════════════════════════════════════════════════════════
A1. Для каждого упражнения БЕЗ визуала — искать на Pixabay по названию +
    типу движения. Использовать PIXABAY_API_KEY из .env.
    Endpoint: GET https://pixabay.com/api/ (см. их доку).
    Запросы формировать осмысленно, по-английски и по-русски (lang=ru тоже
    пробовать): напр. neck stretch, shoulder rotation, forward bend,
    cat cow stretch, diaphragmatic breathing, side bend spine, hip mobility,
    gentle stretching, isometric exercise. image_type пробовать 'photo' и
    'illustration' и 'vector'; category health/sports/people.
A2. КРИТИЧНО — брать только точное совпадение движения с нашей техникой.
    Лучше НЕ поставить, чем поставить картинку, где человек делает ДРУГОЕ.
    Это принцип из прошлых тикетов, соблюдать. Кот сам оценивает совпадение
    по названию/описанию результата; при сомнении — не берёт, идёт в SVG (блок B).
A3. Лицензия Pixabay:
    - НЕ хотлинкать — СКАЧАТЬ файл к себе в public/exercises/ (их URL временный).
    - Добавить атрибуцию: где показывается картинка с Pixabay — подпись/ссылка
      «фото: Pixabay» (мелко, ненавязчиво, в карточке или в info). Требование
      их лицензии.
    - Записать источник каждого файла в docs/IMAGE_SOURCES.md (имя, автор если
      есть, ссылка, дата).
    - Кэш 24ч, без массовых автозапросов, лимит 100/60сек — вписаться.
A4. Вес: картинка <150 КБ, гиф <500 КБ. Оптимизировать (ресайз, сжатие).
    Привязать к exercises.image_url / gif_url миграцией/сидом.

═══════════════════════════════════════════════════════════════════
БЛОК B — РИСОВАННЫЕ SVG-СХЕМЫ НА ОСТАЛЬНЫЕ
═══════════════════════════════════════════════════════════════════
B1. На упражнения, где живого совпадения не нашлось — нарисовать SVG-схему
    движения: простая человеческая фигурка + СТРЕЛКИ направления движения
    (как на инструкциях к тренажёрам). Понятно показывает суть: наклон
    вперёд, поворот головы, подъём руки, круг плечом, кошка-корова и т.д.
B2. Единый стиль всех схем: одна фигурка, одна толщина линий, стрелки
    брендовым акцентом. Перекрашиваются под тему через currentColor/CSS-
    переменные (как логотип). Лёгкие (inline SVG или файл <20 КБ).
B3. Схемы генерировать/хранить системно: либо набор SVG-файлов в
    public/exercises/svg/, либо компонент-конструктор по параметрам движения
    (наклон/поворот/подъём + сторона + амплитуда). Реши по факту, но чтобы
    на каждое из оставшихся упражнений схема реально была, не пустой квадрат.
B4. Привязать схему к упражнению (поле или логика в exercise-card: gif →
    image → svg-схема → и только в самом крайнем случае старый placeholder).

═══════════════════════════════════════════════════════════════════
БЛОК C — ЛОГИКА ПОКАЗА
═══════════════════════════════════════════════════════════════════
C1. Приоритет в карточке: живой gif/картинка (если есть и точная) → иначе
    SVG-схема движения → placeholder по типу только если совсем ничего.
    После этого тикета placeholder «знак типа» должен остаться лишь как
    аварийный fallback (в норме у всех 50 — гиф или схема).
C2. Проверить, что на всех 50 упражнениях (и в каталоге /app/exercises, и
    в карточке тренировки) визуал реально отображается.
C3. lazy-load + Skeleton сохранить (SPEC 4.3), чтоб не тормозило на 3G.

ГРАНИЦЫ «НЕ ТРОГАТЬ»:
- Токены/secrets не коммитить (PIXABAY_API_KEY только в .env, НЕ в git, НЕ в чат)
- Контент упражнений (ТЕКСТЫ техники) не переписывать — Роман согласует с врачом
- 8 существующих рабочих GIF не ломать (если точные — оставить как есть)
- 2 реальных пользователя, их данные — не трогать
- Автодеплой не ломать, соседний ai-arhitektor-landing не трогать
- Домен не подключать
- Режимы, крон, диагностику, оформление, отзывы — НЕ переделывать (работают)

ОБХОД МИН:
- Pixabay: скачивать файлы, НЕ хотлинкать; атрибуция обязательна; кэш 24ч
- SVG-схемы — единый источник стиля, не плодить разнобой; currentColor под темы
- Tailwind v4 / Next 16 — Context7 не авторизован, сверяйся с node_modules
- Пересобрать apply_all.sql (scripts/build-apply-all.mjs) — чистая база = прод
- Не раздуть вес бандла: картинки оптимизировать, SVG лёгкие, lazy-load

САМОПРОВЕРКА (по закону, перед деплоем):
- npx tsc --noEmit / pnpm lint / pnpm build — зелёные
- ФАКТ: SQL-запрос — сколько из 50 упражнений имеют gif/image, сколько svg,
  сколько осталось на голом placeholder (в идеале 0).
- Живой тест на проде: открыть каталог /app/exercises и тренировку —
  у каждого упражнения виден визуал (гиф или схема), Skeleton при загрузке,
  атрибуция Pixabay где их картинки.
- Вес страниц не взлетел (проверить, что lazy работает).
- 8 старых GIF на месте, 2 юзера целы.
- ГЛАЗАМИ Роману — список что проверить (полистать каталог, начать тренировку).

ДЕПЛОЙ только после зелёных тестов (git push → Actions → Vercel).
Обновить SPEC_gimn_zdorovia_v2.md (приложение D) и docs/IMAGE_SOURCES.md.

═══════════════════════════════════════════════════════════════════
=== CHANGELOG (обязательно) ===
Разведка: сколько было с визуалом / без
Pixabay: сколько живых картинок добавлено, по каким упражнениям, атрибуция
SVG-схемы: сколько нарисовано, где лежат, как привязаны
Итог покрытия: X из 50 гиф/картинка, Y из 50 svg, Z на placeholder (цель Z=0)
Новые/изменённые файлы: путь — что — зачем
Миграция/сид: привязка url, пересборка apply_all
docs/IMAGE_SOURCES.md: обновлён
Тесты: tsc/lint/build + факт покрытия + живой тест
Деплой: commit · push · Actions · прод
Осталось Роману глазами: список
Если встал без Романа — блок «=== НУЖНО ОТ РОМАНА ===» точными шагами.
═══════════════════════════════════════════════════════════════════
