# Локальные секреты — никогда не загружать в Vercel
secrets/
*.env
.env*

# Шаблон без значений: Next тянет его в трассировку функций,
# и правило .env* выше ломает деплой коллизией. Секретов в нём нет.
!.env.example

# CI-конфиги — не публиковать на сайте
.github/
