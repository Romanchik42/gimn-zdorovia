/**
 * Контекстные подсказки (GIMN-029).
 *
 * Четыре темы из батча в основной тур не вошли намеренно: до баллов,
 * второго режима, углублённой диагностики и анкеты общего режима человек
 * доходит в разное время, иногда через недели после первого входа.
 * Рассказ о том, чего сейчас нет на экране, — это не обучение, а шум.
 *
 * Поэтому каждая показывается ровно там и тогда, где становится нужна:
 * при первом открытии своего экрана. И ровно один раз — отметка лежит в
 * users.hints_seen (0028) и переживает смену устройства.
 *
 * Тексты — дословно из батча.
 */

export const HINT_IDS = ["points", "modes", "diagnostics", "general-profile"] as const;
export type HintId = (typeof HINT_IDS)[number];

export type Hint = { title: string; description: string };

export const HINTS: Record<HintId, Hint> = {
  points: {
    title: "🎁 Баллы",
    description: [
      "За каждую активность накапливаете баллы:",
      "• Пригласили друга — 30, если оплатит — 200",
      "• Нашли баг — 50 (после подтверждения)",
      "• Хорошая идея — 20, если войдёт в план — 100",
      "• Стрик 30 дней — 100, 100 тренировок — 500",
      "",
      "Баллы применятся при выборе тарифа,",
      "когда запустим платный режим.",
    ].join("\n"),
  },
  modes: {
    title: "🔄 Два режима",
    description: [
      "• Бехтерева — реабилитация при болезни:",
      "  щадящие упражнения, тесты подвижности",
      "• Форма — общая гимнастика, дом или зал,",
      "  подключается турник и инвентарь",
      "",
      "Переключайтесь без потерь данных —",
      "у каждого режима свой план, история и меню.",
    ].join("\n"),
  },
  diagnostics: {
    title: "🩺 Углублённая диагностика",
    description: [
      "Оценка состояния за 5-7 минут:",
      "• Подвижность позвоночника (тест Шобера)",
      "• Утренняя скованность",
      "• Ощущение боли",
      "",
      "Программа скорректирует нагрузку под ваше",
      "сегодняшнее состояние — если тяжело,",
      "занятие станет мягче автоматически.",
      "",
      "Пройти можно раз в 1-2 недели.",
    ].join("\n"),
  },
  "general-profile": {
    title: '📋 Анкета для режима "Форма"',
    description: [
      "Расскажите о себе — программа настроится",
      "под вас:",
      "• Цель (похудеть / держать / набрать)",
      "• Где занимаетесь (дом / зал)",
      "• Что есть из инвентаря (турник, гантели, штанга…)",
      "• Уровень подготовки",
      "",
      "Ответы можно поменять в любой момент",
      "в настройках профиля.",
    ].join("\n"),
  },
};

export function isHintId(value: string): value is HintId {
  return (HINT_IDS as readonly string[]).includes(value);
}

/** Отметки приходят из JSONB — чужие значения отбрасываем. */
export function parseHintsSeen(value: unknown): HintId[] {
  if (!Array.isArray(value)) return [];
  return value.filter((v): v is HintId => typeof v === "string" && isHintId(v));
}
