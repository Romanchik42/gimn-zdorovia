/**
 * shadcn-компоненты этого пресета импортируют `cn` из пакета `cn`
 * (официальный пакет shadcn-ui: clsx + tailwind-merge, скомпилированные).
 * Ре-экспорт нужен, чтобы прикладной код ходил через @/lib/utils,
 * как предписывает структура проекта в SPEC 0.5.
 */
export { cn } from "cn";
