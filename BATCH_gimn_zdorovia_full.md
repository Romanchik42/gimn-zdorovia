═══════════════════════════════════════════════════════════════════
СКВОЗНОЙ БАТЧ: GIMN-BUILD — Полная сборка Гимн.здоровья (Этапы 1-8)
═══════════════════════════════════════════════════════════════════

Это НЕПРЕРЫВНЫЙ тикет на всю сборку. Ты идёшь по 8 этапам подряд,
САМ, без остановок на подтверждение. Роман спит — его согласие на
переход между этапами НЕ требуется. Работай на автомате.

───────────────────────────────────────────────────────────────────
ГЛАВНЫЙ ЦИКЛ (для КАЖДОГО этапа, без исключений):
───────────────────────────────────────────────────────────────────
  1. Прочитай нужные блоки SPEC.md (источник истины).
  2. Сделай работу этапа.
  3. САМОПРОВЕРКА: npx tsc --noEmit · pnpm lint · pnpm build
     — все три ЗЕЛЁНЫЕ (lint не хуже baseline).
  4. Если красное — чини, пока не зелёное. НЕ деплой красное.
  5. ДЕПЛОЙ: git add + commit "Этап N: <название>" + git push origin main.
     Автодеплой (GitHub Actions) выкатит на Vercel сам.
  6. Дождись, проверь https://gimn-zdorovia.vercel.app отвечает без 500.
  7. Запиши CHANGELOG этапа (формат — в конце тикета).
  8. БЕЗ ПАУЗЫ переходи к следующему этапу.

Код на диске ≠ код на проде. После каждого этапа — деплой обязателен.

───────────────────────────────────────────────────────────────────
ПРАВИЛО ОСТАНОВКИ (единственная причина встать):
───────────────────────────────────────────────────────────────────
Останавливайся ТОЛЬКО если без Романа физически не двинуться:
  - нужен клик в чужом браузере (OAuth, установка приложения),
  - нужен ключ/значение, которого нет в F:\Project gimn zdorovia\secrets\.env,
  - внешний сервис требует ручного действия владельца аккаунта.

Тогда:
  - НЕ ломай уже сделанное, задеплой последнее зелёное состояние,
  - собери блок "=== НУЖНО ОТ РОМАНА ===" с ТОЧНЫМ списком шагов
    (что открыть, что нажать, что вставить, что прислать обратно),
  - в конце — CHANGELOG всего, что успел,
  - остановись и жди. Роман придёт утром, скопирует это в чат.

Технические ошибки (tsc/lint/build/сеть/VPN fetch failed) — НЕ причина
останавливаться и звать Романа. Чини сам, ретрай сеть 2-3 раза, ищи
решение. Останавливаешься только на том, что требует ЕГО рук.

Не уверен в факте (путь, поле, имя) — ШАГ 0 разведка: грепни, покажи
выхлоп, не выдумывай.

───────────────────────────────────────────────────────────────────
ШАПКА (источники истины, полными путями):
───────────────────────────────────────────────────────────────────
- Спецификация:  F:\Project gimn zdorovia\SPEC.md  (v2.0 — ЕДИНСТВЕННЫЙ источник истины, читай перед каждым этапом)
- Проект:        F:\Project gimn zdorovia
- Секреты:       F:\Project gimn zdorovia\secrets\.env
- GitHub:        https://github.com/Romanchik42/gimn-zdorovia
- Vercel:        https://gimn-zdorovia.vercel.app (scope medved-roman-s-projects)
- Автодеплой:    GitHub Actions (.github/workflows/deploy.yml — РАБОТАЕТ, логику не трогать)
- Telegram bot:  @gimn_zdorovia_bot (токен в .env)

СТЕК (SPEC 0.2): Next.js 16 App Router, TypeScript strict, Tailwind v4,
shadcn/ui, Supabase (Postgres + Auth), Telegram Bot API, cron-job.org,
pnpm. Звук по умолчанию 'soft'. Три темы: sage (дефолт), terracotta, ocean.

ГЛОБАЛЬНЫЕ ГРАНИЦЫ "НЕ ТРОГАТЬ" (весь тикет):
- .github/workflows/deploy.yml — логику автодеплоя не менять
- secrets/ — не коммитить, не выводить наружу
- Токены Роман сознательно НЕ ротировал — не трогать, не переспрашивать
- Соседний проект ai-arhitektor-landing — не касаться
- Логотип НЕ рисовать (Роман сделает позже; в Этапе 1 и 8 — плейсхолдеры)

ГЛОБАЛЬНЫЙ ОБХОД МИН:
- Tailwind v4 (НЕ v3!) — синтаксис @theme и CSS-переменных другой. Context7.
- Next.js 16 App Router — API меняется, бери актуальное через Context7 (use context7).
- Три темы — единый источник в globals.css, НЕ дублировать цвета по компонентам.
- Единый источник для статусов/справочников — не хардкодить строки по месту.
- RLS включена на КАЖДОЙ таблице (SPEC требует). Anon-key публичный только с RLS.
- Zod-схема одна на клиент+сервер (single source of truth).
- Service_role только server-only. parse_mode в Telegram НЕ использовать.
- Cron-эндпоинты проверяют Authorization: Bearer CRON_SECRET.
- /admin/stats — проверка is_admin на СЕРВЕРЕ (Server Component), не на клиенте.
- Миграции: после SQL — NOTIFY pgrst 'reload schema', потом деплой.

═══════════════════════════════════════════════════════════════════
ЭТАП 1 — BOOTSTRAP + ТЕМЫ + ЗАГЛУШКА
═══════════════════════════════════════════════════════════════════
Читай: SPEC 0.2, 0.5, 4.1, 4.2, 5.9, Приложение A.

ШАГ 0: git log --oneline -5 + ls корня, покажи в чат. index.html (старая
  заглушка) заменяется настоящим Next.js — если мешает create-next-app,
  переименуй, потом удали.

Работа:
- pnpm create next-app@latest . --typescript --eslint --tailwind --src-dir
  --app --import-alias "@/*" --use-pnpm  (сохрани .github/, .gitignore,
  .vercelignore, secrets/, README.md при перезаписи)
- npx shadcn@latest init (neutral, CSS variables: yes)
- npx shadcn@latest add button card dialog input textarea select form label
  badge scroll-area sonner skeleton tabs progress avatar sheet dropdown-menu
  radio-group switch slider separator alert tooltip
- pnpm add zod react-hook-form @hookform/resolvers @supabase/supabase-js
  @supabase/ssr lucide-react server-only next-pwa date-fns recharts
  qrcode.react @dnd-kit/core @dnd-kit/sortable driver.js
- globals.css: три темы ТОЧНО из SPEC 4.2 (sage/terracotta/ocean, data-theme
  на html), transition 0.3s, scroll-behavior smooth. Inter (cyrillic+latin)
  + JetBrains Mono через next/font/google.
- layout.tsx: html lang="ru" data-theme="sage", Toaster richColors top-center,
  метаданные из SPEC 4.1, manifest '/manifest.json'.
- page.tsx (лендинг-заглушка, БЕЗ логотипа): по центру "Гимн.здоровья",
  подзаголовок "гимнастика для здоровья", строка "Реабилитация при болезни
  Бехтерева и поддержание формы. Скоро открытие.", внизу краткий мед.
  дисклеймер (SPEC 5.9). Кнопка-переключатель трёх тем для демонстрации.
  Адаптивно, читаемо на 320px.
- public/manifest.json: name/short_name "Гимн.здоровья", display standalone,
  background_color #FAFAF7, theme_color #7C9885, иконки 192/512 — плейсхолдеры.
  next-pwa в next.config без агрессивного кеша.
- .env.example: ВСЕ переменные из SPEC 3.9 шаблоном (без реальных значений).

Тесты → деплой → CHANGELOG → Этап 2.

═══════════════════════════════════════════════════════════════════
ЭТАП 2 — БД + AUTH
═══════════════════════════════════════════════════════════════════
Читай: SPEC 2.1, 2.2, 2.3, 2.4, 3.2-3.4, БЛОК 5.1.

⚠️ КЛЮЧЕВАЯ МИНА: миграции применяются к боевой Supabase. Нужны реальные
ключи из secrets/.env. Проверь, что в .env есть SUPABASE_URL,
SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY (Vercel-имена с NEXT_PUBLIC_
уже смаплены в GIMN-003). Если Supabase project_ref не подключён к Supabase
CLI / MCP — примени миграции доступным способом:
  - если Supabase MCP подключён — через него,
  - иначе через psql/supabase db push с connection string из .env,
  - если connection string нет и получить нельзя без Романа — СТОП,
    вынеси в "НУЖНО ОТ РОМАНА": "дай Database connection string из
    Supabase → Settings → Database → Connection string (URI)".

Работа:
- 9 миграций в supabase/migrations/ (SPEC 2.2, 2.3): users (+ все поля 2.0:
  theme, sounds, tour, referral_code, referred_by, next_report_date,
  is_admin), user_diagnostics, user_profiles_general, exercises,
  workout_sequences, user_week_plan, user_custom_workouts, user_workouts,
  workout_feedback, side_effect_events, side_effect_rules, meals, user_meals,
  shopping_list, user_progress, personal_reports, referrals, referral_clicks,
  notifications_log. RLS на КАЖДОЙ. Триггеры updated_at. COMMENT ON.
- supabase/seed.sql: ~50 упражнений (35 Бехтерева из аудита в SPEC + 15
  общих), ~40 блюд (топ-30 продуктов SPEC 5.3), ~15 правил Decision Tree
  (SPEC 5.2), ~14 шаблонов последовательностей (SPEC 5.4). Упражнения с
  полями: type, target_joint, mode, description, technique, duration/reps,
  level, contraindications, side_effects.
- src/lib/env.ts (Zod-валидация env), src/lib/supabase/{admin,client,server}.ts,
  src/lib/schemas/ (user, diagnostics, workout-feedback, nutrition).
- Supabase Auth: Telegram Login Widget (верификация hash HMAC-SHA256,
  SPEC 3.1) + Email/Password резерв.
- src/lib/referral/code-generator.ts (SPEC 2.4, алфавит без похожих
  символов, коллизии — ретрай до 5 раз).
- /auth/login, /auth/register. При регистрации — генерация referral_code,
  чтение referred_by из localStorage если есть.
- POST /api/auth/telegram (SPEC 3.1).

После миграций: NOTIFY pgrst 'reload schema'. Тесты → деплой → CHANGELOG → Этап 3.

═══════════════════════════════════════════════════════════════════
ЭТАП 3 — ОНБОРДИНГ
═══════════════════════════════════════════════════════════════════
Читай: SPEC US-01, US-02, 3.2, БЛОК 5.1, 5.4.

- /onboarding/welcome: расшифровка "гимнастика для здоровья" + мед.
  дисклеймер (SPEC 5.9), кнопка "Понятно".
- /onboarding/mode: выбор Бехтерева / Общая форма.
- /onboarding/behtereva: 5 экранов (тест Шобера, боковой наклон, ротация,
  зоны боли мультивыбор, скованность+давление). Каждый пропускаем "не знаю".
- /onboarding/general: пол, возраст, вес, рост, цель, активность, сложность,
  дни тренировок. Zod: вес 30-300, рост 100-250, возраст 14-100.
- /onboarding/theme: выбор sage/terracotta/ocean с живым превью.
- POST /api/onboarding/diagnostics (расчёт intensity + focus, SPEC 3.2),
  POST /api/onboarding/general (расчёт BMR→TDEE→target, Миффлин SPEC 5.1).
- Автосоздание user_week_plan по умолчанию (SPEC 5.4) под режим.

Тесты → деплой → CHANGELOG → Этап 4.

═══════════════════════════════════════════════════════════════════
ЭТАП 4 — ТРЕНИРОВКИ
═══════════════════════════════════════════════════════════════════
Читай: SPEC US-03, US-04, 3.3, 4.3, 5.2.

- /app: главный экран (приветствие, карточка "Сегодня" + кнопка Начать,
  свёрнутое меню дня, полоска регулярности, bottom-nav). data-tour атрибуты
  на элементы (для Этапа 8).
- /app/workout/[id]: универсальная карточка (SPEC 4.3) — GIF/картинка со
  стрелками, название, длительность/повторы, техника, warning если есть,
  три кнопки ✅/⚠️/❌ высотой ≥56px, порядок фиксирован. Автопереход 500мс.
  Возврат "назад". Частичная тренировка сохраняется.
- POST /api/workout/generate (SPEC 3.3): выбор sequence по mode+day+intensity,
  раскрытие в упражнения, фильтр противопоказаний по диагностике.
- POST /api/workout/feedback (SPEC 3.4): отметка, при difficult — проверка
  side_effects.
- POST /api/workout/side-effect: модалка симптомов → Decision Tree (SPEC 5.2,
  правила из БД side_effect_rules), советы, next_workout_adjustment.
  Правило "3 побочки за неделю → баннер врача".
- GIF пока нет — плейсхолдер-картинка + техника текстом (edge case 2).

Тесты → деплой → CHANGELOG → Этап 5.

═══════════════════════════════════════════════════════════════════
ЭТАП 5 — КОНСТРУКТОР + КАТАЛОГ + ПЛАН
═══════════════════════════════════════════════════════════════════
Читай: SPEC US-05, US-06, 3.1, 3.2(custom/save), 3.3(plan/update), 4.4.

- /app/workout/custom: конструктор (фокус чипсы, длительность, интенсивность
  → "Собрать" → список с drag-n-drop @dnd-kit, удаление, добавление из
  каталога, изменение длит/повторов). Кнопки "Начать сейчас" / "Сохранить
  как шаблон". Для Бехтерева — противопоказанные упражнения серым + ⚠️ +
  подтверждение при добавлении (SPEC US-05, НЕ блокировать).
- /app/exercises: каталог с фильтрами (сустав, тип, длительность), режим
  выбора для конструктора.
- /app/plan: редактор недельного плана (7 карточек, смена фокуса,
  предупреждения о двух одинаковых группах подряд и отсутствии отдыха —
  предупреждать, не блокировать). Кнопка "Вернуть рекомендуемый план".
- POST /api/workout/custom (SPEC 3.1), /api/workout/custom/save (лимит 20
  шаблонов), /api/plan/update (SPEC 3.3, warnings но сохраняем).

Тесты → деплой → CHANGELOG → Этап 6.

═══════════════════════════════════════════════════════════════════
ЭТАП 6 — ПИТАНИЕ + ПРОГРЕСС
═══════════════════════════════════════════════════════════════════
Читай: SPEC US-07, US-09, 3.5, 5.3, 5.5.

- /app/nutrition: вкладки Сегодня / Неделя / Покупки. Блюда с калоражем и
  БЖУ, раскрытие рецепта (граммы, шаги, время). Список покупок с граммами
  и галочками "куплено".
- POST /api/nutrition/generate (SPEC 3.5): подбор под target±5%, продукты
  из топ-30 (SPEC 5.3), не чаще 2 раз/нед, учёт противопоказаний,
  агрегация shopping_list.
- /app/progress: графики recharts (гибкость/вес/скованность/регулярность),
  блок "Рекомендация системы" из personal_reports, "Следующий отчёт: дата".
- /app/history: история занятий и отметок.

Тесты → деплой → CHANGELOG → Этап 7.

═══════════════════════════════════════════════════════════════════
ЭТАП 7 — TELEGRAM + КРОН + PWA
═══════════════════════════════════════════════════════════════════
Читай: SPEC 3.6, 3.7, 5.5, 5.10, 5.11, US-08.

- POST /api/telegram/webhook: команды /start (с ?start=REFCODE),
  /today, /menu, /progress, /invite (ссылка+QR картинкой), /help.
  parse_mode НЕ использовать. Верификация secret_token.
- 3 крон-эндпоинта с проверкой Authorization Bearer CRON_SECRET:
  /api/cron/morning-reminder, /api/cron/evening-reminder,
  /api/cron/personal-report (SPEC 3.6, 5.5 — отчёт от даты регистрации,
  next_report_date += 30, крутится ежедневно 09:00 но берёт только
  сегодняшних).
- Напоминания настраиваются в настройках (время утро/вечер, вкл/выкл).
- PWA: manifest финализировать, service worker (next-pwa), оффлайн —
  последняя тренировка из кеша, отметки локально + синк.
- Настройка webhook: если TELEGRAM_WEBHOOK_SECRET есть в .env — выполни
  setWebhook curl'ом (SPEC 5.11). Если нет — сгенерируй секрет, впиши в
  .env локально И в Vercel env, потом setWebhook.
- cron-job.org — кот НЕ имеет доступа к аккаунту Романа. Сгенерируй готовую
  инструкцию (3 задачи, точные URL, заголовки, расписания из SPEC 5.10) в
  блок для Романа — он настроит утром. Это НЕ блокирует остальные этапы:
  крон-эндпоинты работают, их просто некому дёргать до настройки.

Тесты → деплой → CHANGELOG → Этап 8.

═══════════════════════════════════════════════════════════════════
ЭТАП 8 — ТУР + ПОДЕЛИТЬСЯ + ЗВУКИ + НАСТРОЙКИ + АДМИНКА
═══════════════════════════════════════════════════════════════════
Читай: SPEC US-10..US-14, 4.5, 4.6, 4.7, 5.6, 5.7, БЛОК 7.

- Тур (driver.js): app-tour (6 шагов по data-tour элементам) + workout-tour
  (3 шага про кнопки ✅/⚠️/❌). Запуск если !tour_completed. Кнопка "показать
  заново" в настройках. POST /api/tour/complete. Стилизация под тему.
- Модалка "Поделиться" (SPEC 4.6): QR (qrcode.react, логотип-плейсхолдер в
  центре, цвет темы), "Сохранить картинку" PNG 1024×1024, ссылка /i/CODE +
  копировать, navigator.share() если доступен, счётчик приглашённых.
- /i/[code]: страница приглашения, POST /api/referral/track (БЕЗ IP),
  код в localStorage, плашка "тебя пригласил X".
- Звуки (SPEC 5.6): скачать 3 набора (soft/energetic/minimal) по 5 звуков с
  CC0-стоков (Pixabay/Mixkit/Freesound — запросы в SPEC 5.6), MP3 <30КБ,
  нормализация. src/lib/sound/ (player Web Audio + packs). Настройки: вкл/
  выкл, выбор набора (дефолт soft), громкость, "▶ послушать". Уважать
  системный silent mode. Если звуки не скачались/не нашлись подходящие —
  положи тихие заглушки-плейсхолдеры и вынеси в блок Роману "заменить звуки",
  НЕ блокируй этап.
- /app/settings полностью: профиль, оформление (3 темы + авто-смена), звук,
  напоминания, поделиться, блок автора (SPEC 4.7 — ТОЛЬКО ai-arhitektor.ru,
  НИКАКИХ прямых Telegram-ссылок на Романа), мед. дисклеймер.
- /admin/stats (SPEC US-13): проверка is_admin на СЕРВЕРЕ. Всего юзеров,
  новые за 7д, рефералы, топ пригласивших, по каналам, по режимам. Неадмин
  → 403 → редирект /app. ADMIN_USER_ID из env (если пуст — вынеси Роману
  "после первой регистрации впиши свой user_id в ADMIN_USER_ID").
- GET /api/referral/stats (обычный: своё число + total; админ: расширенное).
- Рекламные плейсхолдеры (og-image, avatar) — простые, замена при логотипе.

Финал: полный прогон tsc/lint/build → деплой → CHANGELOG → ИТОГ.

═══════════════════════════════════════════════════════════════════
ФОРМАТ ОТЧЁТА
═══════════════════════════════════════════════════════════════════
После КАЖДОГО этапа — короткий CHANGELOG:

=== CHANGELOG Этап N ===
Новые файлы: путь — что — зачем
Изменённые: путь — что поменялось
Миграции/seed: список + count строк + RLS подтверждена
API: метод + путь
Env новые: имя + где (локально/Vercel)
Мины: что учтено
Тесты: tsc ✅/❌ · lint ✅/❌ · build ✅/❌
Деплой: commit hash · push ✅/❌ · Actions ✅/❌ · прод отвечает ✅/❌
Осталось Роману глазами: список

В САМОМ КОНЦЕ (после Этапа 8 ИЛИ при остановке) — общий блок:

=== ИТОГ СБОРКИ ===
Этапы выполнены: 1..N (какие зелёные, какие нет)
Прод-URL работает: ✅/❌
Что готово целиком: список
Что осталось недоделано: список с причинами

=== НУЖНО ОТ РОМАНА === (если встал ИЛИ есть ручные действия)
Пронумерованный список ТОЧНЫХ шагов:
1. Открой <точный URL> → нажми <что> → вставь <что> → пришли <что>
2. ...
(cron-job.org 3 задачи, возможно ADMIN_USER_ID, возможно connection string,
возможно замена звуков/логотипа — всё сюда, одним копируемым списком)

=== CHANGELOG (полный, адреса всего нового) ===
Формат строки: путь — что — зачем. Карта адресов для следующего тикета.
═══════════════════════════════════════════════════════════════════
